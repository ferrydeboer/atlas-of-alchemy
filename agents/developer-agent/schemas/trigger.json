{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DeveloperAgentTrigger",
  "description": "Minimal input required to invoke the Developer Agent. The agent resolves the run folder and reads all other inputs from the filesystem.",
  "type": "object",
  "properties": {
    "issue": {
      "type": "integer",
      "description": "GitHub issue number to implement. The agent reads the DesignDoc and WorkItem from the corresponding run folder.",
      "examples": [13]
    }
  },
  "required": ["issue"],
  "additionalProperties": false
}
